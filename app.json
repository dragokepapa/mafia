{
  "name": "true-mafia-bot",
  "description": "Telegram Mafia + Crocodile + Gallows Full Game Bot (r4rdsn fork)",
  "keywords": ["telegram", "bot", "mafia", "crocodile", "gallows", "game"],
  "buildpacks": [
    {
      "url": "heroku/python"
    }
  ],
  "formation": {
    "worker": {
      "quantity": 1,
      "size": "free"
    }
  },
  "env": {
    "TOKEN": {
      "description": "Telegram Bot Token (@BotFather se)",
      "required": true
    },
    "MONGO_URI": {
      "description": "MongoDB Atlas Full Connection URI",
      "required": true
    }
  }
}