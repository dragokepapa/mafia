{
  "name": "mafia-host-bot",
  "description": "Telegram Mafia Bot",
  "buildpacks": [
    {
      "url": "heroku/python"
    }
  ],
  "env": {
    "TOKEN": {
      "description": "Your Telegram Bot Token",
      "required": true
    },
    "MONGO_URI": {
      "description": "MongoDB Atlas URI",
      "required": true
    }
  }
}